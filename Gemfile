source 'https://rubygems.org'

# shit, we're running out of memory a lot with this.
# had to update because Heroku had a sunset on older ruby.
# need to update rails to deal with it but that's gonna be hard.
ruby '2.2.3'

gem 'rails', '3.2.14'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'pg'
gem 'taps'

gem 'ckeditor', '4.0.4'
gem 'paperclip'
gem 'newrelic_rpm'
gem "devise" #used for logins
gem 'aws-s3' #s3 stuff for image upload with paperclip
gem 'aws-sdk', '< 2.0'
gem 'nokogiri'
gem 'memcachier'
gem 'dalli'
gem 'unicorn'
gem 'will_paginate'
gem 'jquery-ui-rails'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.6'
  gem 'coffee-rails', '~> 3.2.2'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
